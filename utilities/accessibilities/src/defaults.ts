/**
 * The default enabled state when `enabled` is not provided.
 */
export const DEFAULT_ENABLED           : boolean = true

/**
 * The default read-only state when `readOnly` is not provided.
 */
export const DEFAULT_READ_ONLY         : boolean = false

/**
 * The default active state when `active` is not provided.
 */
export const DEFAULT_ACTIVE            : boolean = false



/**
 * The default cascading behavior for `enabled` when `cascadeEnabled` is not provided.
 */
export const DEFAULT_CASCADE_ENABLED   : boolean = true

/**
 * The default cascading behavior for `readOnly` when `cascadeReadOnly` is not provided.
 */
export const DEFAULT_CASCADE_READ_ONLY : boolean = true

/**
 * The default cascading behavior for `active` when `cascadeActive` is not provided.
 */
export const DEFAULT_CASCADE_ACTIVE    : boolean = false
