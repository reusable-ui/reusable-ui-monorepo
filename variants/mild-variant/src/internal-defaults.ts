/**
 * A context-level default mild state to apply when no `mild` prop is explicitly provided.
 * 
 * Regulates visual tone when direct configuration is absent.
 */
export const contextDefaultMild : boolean = false;
