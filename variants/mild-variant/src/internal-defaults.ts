/**
 * The default mild state to apply when no `mild` prop is explicitly provided.
 * 
 * Helps regulate visual tone when direct configuration is absent.
 */
export const defaultMild : boolean = false;
