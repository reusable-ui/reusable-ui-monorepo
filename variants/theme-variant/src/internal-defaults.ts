// Types:
import {
    type BasicTheme,
}                           from './types.js'



/**
 * Defines a system default theme applied when no `theme` prop is explicitly provided.
 * 
 * - `'primary'`: Typically used as the baseline or main theme style.
 */
export const systemDefaultTheme : BasicTheme = 'primary';
