/**
 * A context-level default emphasized state to apply when no `emphasized` prop is explicitly provided.
 * 
 * Typically used to control visual prominence when direct configuration is absent.
 */
export const contextDefaultEmphasized : boolean = false;
