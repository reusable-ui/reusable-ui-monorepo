/**
 * The default outlined state to apply when no `outlined` prop is explicitly provided.
 * 
 * Typically used to control visual prominence when direct configuration is absent.
 */
export const defaultOutlined : boolean = false;
