// Utilities:
import {
    mountHeadingStyle,
}                           from '../css-heading-mount.js'



/**
 * @deprecated Use `mountHeadingStyle` instead.
 */
export const mountHeadings = mountHeadingStyle;
