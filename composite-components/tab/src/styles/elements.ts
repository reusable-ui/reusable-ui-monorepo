// elements:
export const tabHeaderElm = '.tabHeader' // one degree specificity to overwrite <List>    component ==> .tab>.tabHeader
export const tabBodyElm   = '.tabBody'   // one degree specificity to overwrite <Content> component ==> .tab>.tabBody
export const tabPanelElm  = '.tabPanel'  // one degree specificity to overwrite <Generic> component ==> .tab>.tabBody>.tabPanel
