/** 
 * Baseline color for the regular (default) variant.
 * Pure blue in OKLCH space.
 */
export const regularBaseColor  = 'oklch(0.5 0.3 265 / 1)';

/** 
 * Baseline color for the outlined variant.
 * Fully transparent in OKLCH space.
 */
export const outlinedBaseColor = 'oklch(0 0 0 / 0)';

/** 
 * Baseline color for the mild variant.
 * Light blue in OKLCH space.
 */
export const mildBaseColor     = 'oklch(0.7 0.2 265 / 1)';



/**
 * Baseline color for the valid state.
 * Pure green in OKLCH space.
 */
export const validRegularBaseColor  = 'oklch(0.5 0.3 120 / 1)';

/**
 * Baseline color for the valid state.
 * Fully transparent in OKLCH space.
 */
export const validOutlinedBaseColor = 'oklch(0 0 0 / 0)';

/**
 * Baseline color for the valid state.
 * Light green in OKLCH space.
 */
export const validMildBaseColor     = 'oklch(0.7 0.2 120 / 1)';



/**
 * Baseline color for the valid state.
 * Pure red in OKLCH space.
 */
export const invalidRegularBaseColor  = 'oklch(0.5 0.3 0 / 1)';

/**
 * Baseline color for the invalid state.
 * Fully transparent in OKLCH space.
 */
export const invalidOutlinedBaseColor = 'oklch(0 0 0 / 0)';

/**
 * Baseline color for the invalid state.
 * Light red in OKLCH space.
 */
export const invalidMildBaseColor     = 'oklch(0.7 0.2 0 / 1)';
