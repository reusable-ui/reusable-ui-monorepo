// elements:
export const iconElm    = '.icon'         // one degree specificity to overwrite <Icon> component
export const bodyElm    = ':where(.body)' // zero degree specificity to be easily overwritten
export const controlElm = '.control'      // one degree specificity to overwrite <Control> component
