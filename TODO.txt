[high prior] on|off|inherit of:
[done] gradientable (inheritable)  => on|off|inherit
[done] outlineable  (inheritable)  => on|off|inherit

[done] mildable  (not inheritable) => on|off|inherit
[todo] fix mild on <EditableTextControl> when hovered

[done] nudible   (not inheritable) => no change
excitable (not inheritable) => no change

then test:
usesProgressBarInheritMildVariant
usesListItemInheritMildVariant



[high prior] colorable => themed => flip regular & alternate
[high prior] <ListItem> can overwrite gradient, outlined, mild



[low prior ] fix <Carousel> fast clicking [prev] & [next] button
[low prior ] fix <Carousel> touch scroll sync with <Navscroll>
[low prior ] test tree shake a Dropdown without Modal

[deferred  ] fix [aria-orientation] => from logical isOrientationBlock => convert to physical isOrientationVertical on <List> & <Range>
[deferred  ] fix map physical isOrientationVertical on the handleMouse, handleKeyboard, flip trackUpper & trackLower



group sequences:
// (layouts|positions|appearances|sizes|accessibilities|backgrounds|foregrounds|borders|spacings|typos|states|children|customize):

// layouts:

// positions:

// appearances:

// variants:

// classes:

// styles:

// sizes:

// scrolls:

// accessibilities:

// validations:

// formats:

// behaviors:

// backgrounds:

// foregrounds:

// borders:

// animations:

// spacings:

// typos:

// states:

// children:

// customize:
