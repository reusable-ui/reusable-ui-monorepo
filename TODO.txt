[med prior ] fix preventing default scoll implementation on Firefox

[deferred  ] fixed minor animation_abort_effect in active-as-click
[deferred  ] upgrade onAnimationEnd detection on animating-state/excitable-state (side effect of *hack* on active-as-click)

[deferred  ] fix [aria-orientation] => detect a physical orientation by detecting the writing-mode
[deferred  ] fix <NavPrevItem|NavNextItem> based on <List>'s [aria-orientation]
[deferred  ] fix length accouracy of <ProgressBar value={70}> vs <ProgressBar value={30}> + <ProgressBar value={40}>
[deferred  ] fix <Tooltip outlined={true}> <Arrow>

[unresolved] unknown bug: `ResizeObserver loop limit exceeded` of test-dropdown

[next ver  ] register css_vars of animation of: excitable|collapsible|disableable|activatable|focusable|interactable|clickable|invalidable|etc...



group sequences:
// (layouts|positions|appearances|sizes|accessibilities|backgrounds|foregrounds|borders|spacings|typos|states|children|customize):

// layouts:

// positions:

// appearances:

// variants:

// classes:

// styles:

// sizes:

// scrolls:

// accessibilities:

// forms:

// values:

// validations:

// formats:

// behaviors:

// backgrounds:

// foregrounds:

// borders:

// animations:

// spacings:

// typos:

// states:

// floatable:
// global stackable:

// components:

// children:

// customize:
