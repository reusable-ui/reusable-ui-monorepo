[med prior ] register css_vars of animation of: excitable|collapsible|disableable|activatable|focusable|interactable|clickable|invalidable|etc...

[low prior ] fix <Carousel> fast clicking [prev] & [next] button
[low prior ] fix <Carousel> touch scroll sync with <Navscroll>

[low prior ] fix [aria-orientation] => from logical isOrientationBlock => convert to physical isOrientationVertical on <List> & <Range>
[low prior ] fix map physical isOrientationVertical on the handleMouse, handleKeyboard, flip trackUpper & trackLower
[low prior ] fix <NavPrevItem|NavNextItem> based on <List>'s physical orientation

[deferred  ] fix <HamburgerMenuButton> animation (side effect of *hack* on active-as-click)
[deferred  ] fixed minor animation_abort_effect in active-as-click
[deferred  ] upgrade onAnimationEnd detection on animating-state/excitable-state (side effect of *hack* on active-as-click)

[deferred  ] fix length accouracy of <ProgressBar value={70}> vs <ProgressBar value={30}> + <ProgressBar value={40}>

[deferred  ] fix <Tooltip outlined={true}> <Arrow>

[unresolved] unknown bug: `ResizeObserver loop limit exceeded` of test-dropdown



group sequences:
// (layouts|positions|appearances|sizes|accessibilities|backgrounds|foregrounds|borders|spacings|typos|states|children|customize):

// layouts:

// positions:

// appearances:

// variants:

// classes:

// styles:

// sizes:

// scrolls:

// accessibilities:

// forms:

// values:

// validations:

// formats:

// behaviors:

// backgrounds:

// foregrounds:

// borders:

// animations:

// spacings:

// typos:

// states:

// floatable:
// global stackable:

// components:

// children:

// customize:
