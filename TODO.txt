[high prior] add [enabled] property to <Details>
[high prior] protect size='md' from <parent size='sm|lg'>

[med prior ] rename themable to themeable
[med prior ] rename onFullyExpanded and onFullyCollapsed to onExpanded(Start|End) and onCollapsed(Start|End)

[med prior ] fix border separator on <Accordion|Details|Similar> when outlined={true}
[med prior ] fix <HamburgerMenuButton> animation
[med prior ] fix <NavPrevItem|NavNextItem> arrow when <parent orientation='block'>
[med prior ] fix <List listStyle='tab' outlined={true}> the unselected ones should appear outlined & the whole <Tab> should have underline

[med prior ] register css_vars of animation of: excitable|collapsible|disableable|activatable|focusable|interactable|clickable|invalidable|etc...
[med prior ] add autoCapitalize implementation
[med prior ] add bird view z-Index stack

[low prior ] fix <Carousel> fast clicking [prev] & [next] button
[low prior ] fix <Carousel> touch scroll sync with <Navscroll>
[low prior ] fix length accouracy of <ProgressBar value={70}> vs <ProgressBar value={30}> + <ProgressBar value={40}>


[deferred  ] fixed minor animation_abort_effect in active-as-click
[deferred  ] upgrade onAnimationEnd detection on excitable state (side effect of *hack* on active-as-click)
[deferred  ] upgrade onAnimationEnd detection on animating-state (side effect of *hack* on active-as-click)

[deferred  ] fix [aria-orientation] => from logical isOrientationBlock => convert to physical isOrientationVertical on <List> & <Range>
[deferred  ] fix map physical isOrientationVertical on the handleMouse, handleKeyboard, flip trackUpper & trackLower

[deferred  ] fix <Tooltip outlined={true}> <Arrow>



group sequences:
// (layouts|positions|appearances|sizes|accessibilities|backgrounds|foregrounds|borders|spacings|typos|states|children|customize):

// layouts:

// positions:

// appearances:

// variants:

// classes:

// styles:

// sizes:

// scrolls:

// accessibilities:

// forms:

// values:

// validations:

// formats:

// behaviors:

// backgrounds:

// foregrounds:

// borders:

// animations:

// spacings:

// typos:

// states:

// floatable:
// stackable:

// components:

// children:

// customize:
