[high prior] fix <Carousel outlined={true}> on the navButton
[high prior] fix <Progress mild={true}> should affected to <ProgressBar>

[deferred] fix hack _use***

[deferred] fix [aria-orientation] => from logical isOrientationBlock => convert to physical isOrientationVertical on <List> & <Range>
[deferred] fix map physical isOrientationVertical on the handleMouse, handleKeyboard, flip trackUpper & trackLower

[low prior] fix <Carousel> fast clicking [prev] & [next] button
[low prior] fix <Carousel> touch scroll sync with <Navscroll>

[low prior] test tree shake a Dropdown without Modal

fix fooElm constant with zero specificity:
^(export\s+)?const\s+\w+Elm\s*=\s*'.*?
^(export\s+)?const\s+\w+Elm\s*=\s*'(?::where\(|\*).*?
^(export\s+)?const\s+\w+Elm\s*=\s*'(?!:where\(|\*).*?

add fluid/fixed <GroupItem>

merge fooComponent.fooStyle & fooStyle
\w+Style\s*:\s*\w+Component\.props\.\w+Style\s*\?\?\s*\w+Style,?
merged\w+Style

TODO:
@reusable-ui/clickable => handle space & enter as click


fix touchStart/End on <ActionControl>
fix press/release animation on keyboard press on <Range>

add <AccordionItemGroup maxExpanded={1}>



group sequences:
// (layouts|positions|appearances|sizes|accessibilities|backgrounds|foregrounds|borders|spacings|typos|states|children|customize):

// layouts:

// positions:

// appearances:

// variants:

// classes:

// styles:

// sizes:

// scrolls:

// accessibilities:

// validations:

// formats:

// behaviors:

// backgrounds:

// foregrounds:

// borders:

// animations:

// spacings:

// typos:

// states:

// children:

// customize:
