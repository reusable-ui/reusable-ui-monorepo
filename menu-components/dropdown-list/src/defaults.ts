// reusable-ui components:
import {
    // defaults:
    defaultOrientationableOptions,
}                           from '@reusable-ui/list'            // represents a series of content



// defaults:
export const listDefaultOrientationableOptions = defaultOrientationableOptions;
